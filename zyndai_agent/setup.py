from setuptools import setup, find_packages

setup(
    name="zyndai_agent",
    version="0.0.1",
    description="Stub zyndai_agent for Policy Navigator (deployment shim)",
    packages=find_packages(),
    include_package_data=True,
)
