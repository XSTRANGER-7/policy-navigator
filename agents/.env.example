# ============================================================
#  POLICY NAVIGATOR — Environment Variables
#  Copy this file to agents/.env and fill in your values.
#  On Railway: set these as Service Variables (not a file).
# ============================================================

# ── ZyndAI Protocol ──────────────────────────────────────────
ZYND_API_KEY=zynd_your_api_key_here

# ── OpenAI (used by Eligibility, Matcher, Apply, Form16 agents) ──
OPENAI_API_KEY=sk-your_openai_key_here

# ── Supabase (used by Policy & Apply agents) ─────────────────
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_anon_key_here

# ── x402 Micropayments (Form 16 Premium Agent) ───────────────
PAYMENT_WALLET_ADDRESS=0xYourProjectWalletAddressHere
PAYMENT_SECRET=civis-form16-x402-secret-change-me
BASE_RPC_URL=https://mainnet.base.org

# ── Agent Internal Ports (optional — defaults shown) ─────────
# Only override if running agents on custom ports.
# POLICY_AGENT_PORT=5001
# ELIGIBILITY_AGENT_PORT=5002
# MATCHER_AGENT_PORT=5003
# CREDENTIAL_AGENT_PORT=5004
# APPLY_AGENT_PORT=5005
# FORM16_AGENT_PORT=5006
# FORM16_PREMIUM_AGENT_PORT=5007

# ── Inter-Agent URLs (optional — set when agents are separate Railway services) ──
# Leave unset when all agents run in the same Railway service (default).
# POLICY_AGENT_URL=https://policy-agent.up.railway.app
# ELIGIBILITY_AGENT_URL=https://eligibility-agent.up.railway.app
# MATCHER_AGENT_URL=https://matcher-agent.up.railway.app
# CREDENTIAL_AGENT_URL=https://credential-agent.up.railway.app
# APPLY_AGENT_URL=https://apply-agent.up.railway.app
# FORM16_AGENT_URL=https://form16-agent.up.railway.app
# FORM16_PREMIUM_AGENT_URL=https://form16-premium-agent.up.railway.app
